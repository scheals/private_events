<h1><%= @user.email %></h1>
<ul>Those are the events you've created:</ul>
  <% @user.created_events.each do |e|%>
    <li>
      <p><%= e.name %></p>
      <p><%= e.location %></p>
      <p><%= e.date %></p>
    </li>
  <% end %>

<div>
  <%= link_to 'Home', root_path %>
</div>
